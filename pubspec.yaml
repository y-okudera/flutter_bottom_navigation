name: flutter_bottom_navigation
description: Handle flutter bottom navigation bar.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.14.0 <3.0.0'
  flutter: '>=2.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  flutter_hooks: ^0.18.1
  hooks_riverpod: ^1.0.2
  auto_route: ^3.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  build_runner: ^2.0.4
  auto_route_generator: ^3.1.0

# The following section is specific to Flutter.
flutter:
  uses-material-design: true
